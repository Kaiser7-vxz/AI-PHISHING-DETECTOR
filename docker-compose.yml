version: '3.8'

services:
  phishing-detector:
    build: .
    container_name: ai-phishing-detector
    stdin_open: true
    tty: true
    volumes:
      # Optional: Mount a volume for persistent model storage
      - ./model:/app/model
    command: --help